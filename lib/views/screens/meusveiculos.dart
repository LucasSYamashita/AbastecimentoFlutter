import 'package:flutter/material.dart';

class MyVehiclesView extends StatelessWidget {
  const MyVehiclesView({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Tela de Meus Veículos',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
